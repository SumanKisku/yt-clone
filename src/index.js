import { render } from 'react-dom';
import App from './App.js';
import "./style.css"

render(<App />, document.getElementById('root'));

// As we can see in new react 28 there's no need import "react" in react 18 render function in enough